% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trendfilter_pointprocess.R
\name{trendfilter_pointprocess}
\alias{trendfilter_pointprocess}
\title{Trend Filtering for Point Process Intensity Estimation}
\usage{
trendfilter_pointprocess(
  data,
  A,
  B,
  k = 0,
  lambda = 1,
  rho_scale = 1e-08,
  max_iter = 100,
  tol = 1e-05,
  newton_max_iters = 50,
  newton_tol = 1e-05
)
}
\arguments{
\item{A}{Numeric, start of the observation window.}

\item{B}{Numeric, end of the observation window.}

\item{k}{Integer, order of the trend filter (e.g., 0 = piecewise constant, 1 = piecewise linear).}

\item{lambda}{Numeric, regularization parameter (default = 1).}

\item{rho_scale}{Numeric, scaling factor for ADMM penalty parameter (default = 1e-8).}

\item{max_iter}{Integer, maximum ADMM iterations (default = 100).}

\item{tol}{Numeric, tolerance for ADMM convergence (default = 1e-5).}

\item{newton_max_iters}{Integer, maximum Newton iterations (default = 50).}

\item{newton_tol}{Numeric, tolerance for Newton solver (default = 1e-5).}

\item{x}{Numeric vector of event times.}
}
\value{
A list containing estimated coefficients, intensity values, diagnostics, etc.

When \code{k = 0}, performs total variation denoising (TV denoising);
otherwise, calls the Rcpp backend \code{trendfilter_pointProcess()}.
}
\description{
This function performs trend filtering
on a Poisson point process to estimate the log-intensity function.
}
\examples{
\dontrun{
  # Example data
  set.seed(1)
  x <- sort(runif(50, 0, 1))
  result <- trendfilter_pointprocess(x, k = 1, A = 0, B = 1, lambda = 0.1)
  print(result$theta)
}
}
